<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/archived/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- each archived file, size max 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- total size of all archive files, if total size > 20GB, it will delete old archived file -->
            <totalSizeCap>20GB</totalSizeCap>
            <!-- 60 days to keep -->
            <maxHistory>60</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%date{UTC} %level [%thread] %logger{10} [%file:%line] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CONSOLE"/>
    </appender>

    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
    </appender>

    <springProfile name="local">
        <logger name="io.edpn" level="trace" additivity="false">
            <appender-ref ref="ASYNC_CONSOLE" />
        </logger>

        <root level="info">
            <appender-ref ref="ASYNC_CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="dev">
        <logger name="io.edpn" level="trace" additivity="false">
            <appender-ref ref="ASYNC_FILE" />
        </logger>

        <root level="info">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <springProfile name="test">
        <logger name="io.edpn" level="debug" additivity="false">
            <appender-ref ref="ASYNC_FILE" />
        </logger>

        <root level="info">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <springProfile name="acc">
        <logger name="io.edpn" level="info" additivity="false">
            <appender-ref ref="ASYNC_FILE" />
        </logger>

        <root level="info">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <logger name="io.edpn" level="info" additivity="false">
            <appender-ref ref="ASYNC_FILE" />
        </logger>

        <root level="info">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

</configuration>
